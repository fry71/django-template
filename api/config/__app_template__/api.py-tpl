from __future__ import annotations

from ninja import Router

from api.common.schemas import ErrorSchema

router = Router()


@router.get("/", response={200: str, 400: ErrorSchema})
def index(request):
    return "{{ app_name }} API"
